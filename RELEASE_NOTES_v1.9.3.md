# v1.9.3 更新说明

> 发布日期：2026-09-12
> 源码文件：`嗅觉视频下载器1.9.3.py`
> 上一版本：v1.9.2

---

## 一、本版核心变化（v1.9.2 → v1.9.3）

本版是一次**修复 + 体验**并重的更新，主要解决 B 站 412 风控拦截问题，并把 DASH 下载的进度显示改为三阶段平滑推进。

### 1. 修复 B 站 412 风控拦截（重点）

**问题现象**：扫描 B 站视频页面时返回 `412 Client Error: Precondition Failed`，视频无法下载。

**根本原因**：两个因素叠加触发 B 站风控
- 请求头 User-Agent 停留在旧版 Chrome/91，与当前浏览器特征不符
- 请求缺少 `buvid3` 设备指纹 Cookie

**修复方式**：
- 统一升级所有请求头的 User-Agent 至 Chrome/138
- 补齐 `Sec-Fetch-*` 系列浏览器标准请求头
- 新增 `ensure_bilibili_cookies()`：运行时自动生成随机 `buvid3` 设备指纹（大写 UUID + `infoc` 后缀），请求 B 站前自动确保其存在
- 新增 `refresh_bilibili_cookies()`：命中 412 时自动访问 B 站首页刷新 Cookie 后重试

### 2. 移除硬编码的私人 Cookie（安全改进）

旧版代码中硬编码了 `SESSDATA` 等登录凭据与伪造 Cookie 值，存在账号泄露风险。

**本版处理**：
- 移除全部硬编码私人 Cookie，改为运行时生成随机 `buvid3`
- 需要下载 1080P/4K 的用户，改为在代码顶部 `USER_SESSDATA` 变量中填入自己的 SESSDATA，代码中附有详细的获取步骤注释

> 提示：不填写 `USER_SESSDATA` 时最高画质为 720P，这是 B 站对未登录用户的限制，属正常现象。

### 3. DASH 下载三阶段平滑进度条

旧版进度条在视频流、音频流之间跳变，观感突兀。

**本版改进**：将整个下载过程划分为三个连续阶段，进度条平滑递增

| 阶段 | 进度区间 | 说明 |
|------|---------|------|
| 视频流下载 | 0% → 45% | 实时显示已下载量 / 总量 / 速度 |
| 音频流下载 | 45% → 90% | 同上 |
| ffmpeg 合并 | 90% → 100% | 显示"正在用 ffmpeg 合并视频和音频" |

- 每个数据块（chunk）都更新进度，进度条不再跳跃
- 进度条旁实时显示当前所处阶段标签
- 新增 `DownloadProgressWindow.set_combined_progress()` 方法支撑跨阶段进度
- 合并成功 / 失败 / 异常均有明确的终态提示，窗口 2 秒后自动关闭

---

## 二、相比仓库上一版本（v1.2.0）的累积变化

仓库中此前发布的源码对应 **v1.2.0**，本次直接升级至 **v1.9.3**，跨越 13 个版本。以下是累积的能力变化：

### 新增能力

| 能力 | 引入版本 | 说明 |
|------|---------|------|
| B 站专用下载流程 | v1.3.0 | 剪贴板复制 B 站页面链接（BV/AV 号）即自动触发下载 |
| 最高画质提取 | v1.4.0 | 支持 1080P60 / 4K / 8K / HDR / 杜比视界，只下最佳视频+最佳音频 |
| 标题与封面 | v1.5.0 | 自动提取视频标题命名文件，自动下载封面 |
| DASH 音视频合并 | v1.8.0 | 视频流+音频流用 ffmpeg 无损合并为完整 MP4 |
| FFmpeg 自动获取 | v1.9.0 | 未检测到 ffmpeg 时自动下载（约 80MB，仅需一次） |
| 配置记忆 | v1.9.0 | 下载路径与最近日志持久化到 `downloader_config.json` |
| 合并已有文件 | v1.9.2 | 一键扫描并合并此前未合并的视频/音频分片 |
| 独立进度窗口 | v1.3.0 | 剪贴板触发的下载弹出独立进度窗，含实时网速 |

### 重要问题修复

- **v1.6.1** 修复界面"未响应"/崩溃：所有 UI 操作切回主线程，符合 Tkinter 线程模型
- **v1.6.2** 修复"只下载封面、视频下不了"：去除重复的 `captured_urls` 去重拦截
- **v1.6.2** 修复选到 AV1 编码导致播放器打不开：改为 AVC > HEVC > AV1 优先级排序
- **v1.8.0** 修复视频流被误存为 `.m4a`：改按 URL 流 ID 判断视频/音频
- **v1.9.0** 修复 B 站视频下载到错误目录：移除未定义的 `download_path_var`
- **v1.9.3** 修复 412 风控拦截（见上文）

### 体验优化

- 日志颜色分级：成功=绿、错误=红、警告=橙、关键操作=蓝（v1.7.0）
- 点击"扫描网站"即时反馈，避免误以为无响应而重复点击（v1.7.0）
- 剪贴板监控只响应监控启动后新复制的内容（v1.6.0）
- URL 输入框支持回车直接扫描（v1.6.0）

---

## 三、文件说明

仓库中同时保留两个源码版本：

| 文件 | 版本 | 说明 |
|------|------|------|
| `嗅觉视频下载器1.9.3.py` | v1.9.3 | **最新版**，推荐使用 |
| `嗅觉视频下载器 (Video Sniffer Downloader).py` | v1.2.0 | 旧版，仅作版本对照 |

Windows 用户建议直接下载 `.exe` 版本，无需配置 Python 环境。

---

## 四、升级注意事项

1. **首次使用 B 站下载**：程序会自动下载 ffmpeg（约 80MB），请保持网络畅通。国内网络若直连 GitHub 失败，会自动切换 3 个国内镜像源重试。
2. **配置文件**：程序会在同目录生成 `downloader_config.json` 用于记忆下载路径与日志，删除该文件即可恢复默认设置。
3. **画质说明**：不填 `USER_SESSDATA` 时最高 720P，属 B 站未登录限制，非程序缺陷。
4. **依赖安装**：`pip install -r requirements.txt`（`requests`、`pyperclip`）。

---

## 五、完整版本历史

详见 [CHANGELOG.md](CHANGELOG.md)。
